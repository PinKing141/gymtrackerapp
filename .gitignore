node_modules/
dist/
.env
.env.local
.env.*.local

# local Python env used for lightweight static checks in this workspace
.venv/

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log
.firebaserc.local

# Push backend secrets — never commit service-account keys
serviceAccount*.json
server/*service-account*.json
